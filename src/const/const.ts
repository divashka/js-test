export enum AppRoute {
  Root = "/",
  Start = "/start",
  Test = "/test",
  Result = "/result",
  NotFound = "/not-found",
}

export enum SliceNameSpace {
  Test = "TESTS",
  App = "APP",
}

export enum APIRoute {
  Test = "/tests",
}
